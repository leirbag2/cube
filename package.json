{
  "name": "cube",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "build": "npx esbuild app.js --bundle --minify --format=iife --global-name=CubeApp --outfile=dist/app.js && cp index.html styles.css dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.27.3"
  },
  "dependencies": {
    "three": "^0.182.0"
  }
}
